# 프로젝트 기본 지침

## 번드시 준수해야할 원칙

- 모든 커뮤니케이션은 한국어로 한다.
- 오버엔지니어링 금지
- 카드는 순간 이동하지 않고 자연스럽게 이동하도록 애니메이션 처리한다.
- 동일한 카드가 2장으로 보이지 않도록 한다.


## AI 가중치 통계 튜닝 지침

`gostop.html`의 AI 판단 로직(먹을 패/버릴 패 선택, 고/스톱 판단 등)에 쓰이는 가중치를
시뮬레이션으로 통계적으로 튜닝할 때는 다음 원칙을 따른다.

### 최적화 지표: 승률이 아니라 평균 획득 점수/코인

후보 가중치가 더 나은지 판단할 때 라운드 승리 확률(승률)이 아니라 평균 획득
점수/코인을 기본 지표로 삼는다. 어떤 전략은 이기는 판 수는 더 적어도(예: 3고 이상
배율을 노리다 실패하는 판이 늘어도), 성공했을 때 훨씬 크게 먹어서 평균 획득량은 더
높을 수 있다. 승률만으로 평가하면 이런 고위험·고수익 판단을 놓치거나 오히려
불리하다고 오판할 수 있다. 승률은 최종 검증 단계에서 보조 지표로만 확인한다(주
지표가 아님).

### 예외: 파산(게임 종료) 임박 시에는 상대적 코인 우열에 따라 판단

위 원칙의 유일한 예외는 누군가의 코인이 거의 소진되어 게임이 끝날 수 있는 상황이다.
이때는 평균 점수 극대화가 아니라, 내가 상대보다 코인이 많은지 적은지(상대적 우열)에
따라 다르게 판단해야 한다 — 앞서 있으면 보수적으로, 뒤처져 있으면 공격적으로.

`aiDecideGoStop` 함수는 이미 이 원칙을 두 군데에 반영하고 있으므로, 새로 튜닝할 때도
이 구조를 유지한다:

1. 최상단 파산 단락: 지금 스톱하면 실제로 누군가 파산해서 게임이 그 자리에서 끝나는
   상황이면, 점수 계산과 무관하게 `iAmFinalWinner`(내가 최종 1위인지)만으로 즉시
   go/stop을 결정한다.
2. `relStanding` 항: 코인이 상대 평균보다 앞서 있으면 goChance를 낮추고(보수적),
   뒤처져 있으면 높인다(공격적).

시뮬레이션 하네스를 새로 만들거나 튜닝을 다시 돌릴 때도, 이 두 장치를 순수 평균 점수
극대화 목표로 대체하지 말고 그대로 유지한 채 주변 가중치만 조정한다.

### cardWeight 비교 기준값(고정 베이스라인)

`cardWeight()`(카드 종류별 기본 가중치)를 통계적으로 튜닝할 때 "기존값"과 비교하라고
하면, 그때그때의 현재 라이브 값이 아니라 **커밋 `baa9eb4`(이 프로젝트에서 첫 튜닝이
시작되기 직전) 시점의 값을 고정 베이스라인**으로 삼는다:

- 광 = 5(12월 비광만 2.8)
- 쌍피 = 5, 피 = 3
- 단achievable(그 색 단이 아직 완성 가능) = 2
- 단dead(더는 완성 불가능) = 1.2(+`flatThresholdBonus`)
- 고도리achievable(새 열끗, 고도리 완성 가능) = 1.2
- 열끗dead(고도리 아니거나 이미 완성 불가능) = 1.25(+`flatThresholdBonus`)

현재 라이브 값은 이후 여러 차례 튜닝을 거치며 이 베이스라인과 달라져 있다(`gostop.md`의
`cardWeight` 절에 각 라운드별 변경 이력이 정리돼 있음). 사용자가 별도로 다른 기준을
지정하지 않는 한, 새 통계 검증은 항상 이 baa9eb4 값을 "기존값(대조군)"으로 사용한다.

### 검증 실행 방법론

구체적인 하네스 코드 템플릿은 `/tune-weight` 스킬(`.claude/skills/tune-weight/SKILL.md`)에
있다. 여기는 그 과정에서 반드시 지켜야 할 원칙만 정리한다.

- **N은 작게 시작해서 애매하면(|z| 1~2대) 반드시 늘려본다.** 고/스톱 판단, 흔들기, 특정
  카드 조합처럼 판별로 드물게만 발생하는 상황은 신호가 크게 희석되므로 N=수만~수십만이
  필요할 수 있다. "효과 없음"으로 단정하기 전에, 검증 대상 상황이 실제로 얼마나 자주
  나오는지부터 재본다 — 세션 안에서 N=5000에서 "효과 없음"으로 보였던 게 N=20000에서
  유의미하게(z=-3.09) 드러난 사례가 있었다.
- **계층화는 검정력을 높이지 않는다.** "결과가 같았던 판(델타=0)들을 빼고 관련 있는 판만
  보면 유의성이 더 잘 나오지 않냐"는 의문이 들 수 있는데, 실측 결과 계층화 표본의 z와
  전체표본의 z는 사실상 같았다(델타=0인 판들이 평균과 분산을 같은 비율로 희석시켜
  상쇄됨). 즉 표본 부족 문제는 계층화가 아니라 그냥 N을 늘려서 풀어야 한다. 계층화는
  "왜 효과가 작게 나오는지" 설명하는 진단 용도로만 쓴다. 계층화 기준을 쓸 거라면 반드시
  A/B 결과 차이와 무관한 기준(예: "그 판단 자체가 발생했는가")이어야 하고, "A와 B 결과가
  실제로 달랐던 판만" 같은 기준은 순환논리라 안 된다.
- **한 방향이 유의미하게 나쁘면, 반대 방향(또는 인접 변형)도 검증한다.** 어떤 신호를
  더 심각하게 보도록 키운 게 유의미하게 나빴다면, 반대로 그 신호를 덜 심각하게 보는
  쪽이 실제 개선일 수 있다 — 세션 안에서 "막판 상대위협 강화"가 유의미하게 나빴을 때
  "막판 상대위협 완화"를 검증해 실제 채택된 개선을 찾은 사례가 있다.
- **채택 여부와 무관하게 모든 시도를 기록한다.** N, z, mean, 채택/기각 이유를
  `gostop.md`의 관련 함수 설명 아래에 "N차 튜닝"으로 번호를 이어 붙여 남긴다 — 기각된
  시도도 다음에 같은 방향을 또 시도해 시간을 낭비하지 않도록 반드시 남긴다.


## 코딩 행동 지침

흔한 LLM 코딩 실수를 줄이기 위한 행동 지침. 프로젝트별 지침과 함께 적용한다.

**트레이드오프:** 이 지침은 속도보다 신중함에 무게를 둔다. 사소한 작업에는 판단력을 발휘해 유연하게 적용한다.

### 1. 코딩 전에 생각하기

**추측하지 말 것. 혼란을 숨기지 말 것. 트레이드오프를 드러낼 것.**

구현하기 전에:
- 가정을 명시적으로 밝힌다. 불확실하면 질문한다.
- 여러 해석이 가능하면 제시한다 — 임의로 하나를 선택하지 않는다.
- 더 단순한 접근이 있다면 언급한다. 근거가 있으면 반박(제안)한다.
- 불명확한 부분이 있으면 멈춘다. 무엇이 헷갈리는지 명확히 밝히고 질문한다.

### 2. 단순함을 최우선으로

**문제를 해결하는 최소한의 코드만 작성한다. 추측성 코드는 없다.**

- 요청받지 않은 기능은 추가하지 않는다.
- 한 번만 쓰이는 코드에 추상화를 두지 않는다.
- 요청하지 않은 "유연성"이나 "설정 가능성"을 넣지 않는다.
- 발생할 수 없는 상황에 대한 에러 처리를 하지 않는다.
- 200줄로 작성했는데 50줄로 될 수 있다면 다시 작성한다.

스스로에게 물어보라: "시니어 엔지니어가 보면 과하다고 할까?" 그렇다면 단순화한다.

### 3. 외과적인 변경

**반드시 필요한 부분만 건드린다. 본인이 만든 것만 정리한다.**

기존 코드를 수정할 때:
- 인접한 코드, 주석, 포맷팅을 "개선"하지 않는다.
- 고장나지 않은 것을 리팩터링하지 않는다.
- 본인 취향과 다르더라도 기존 스타일을 따른다.
- 관련 없는 죽은 코드를 발견하면 언급만 하고 삭제하지 않는다.

변경으로 인해 고아 코드가 생기면:
- 본인의 변경으로 인해 쓰이지 않게 된 import/변수/함수는 제거한다.
- 요청받지 않았다면 원래 있던 죽은 코드는 제거하지 않는다.

기준: 변경된 모든 라인은 사용자의 요청과 직접 연결되어야 한다.

### 4. 목표 지향 실행

**성공 기준을 정의하고, 검증될 때까지 반복한다.**

작업을 검증 가능한 목표로 바꾼다:
- "유효성 검사 추가" → "잘못된 입력에 대한 테스트를 작성한 뒤 통과시킨다"
- "버그 수정" → "버그를 재현하는 테스트를 작성한 뒤 통과시킨다"
- "X 리팩터링" → "리팩터링 전후로 테스트가 통과함을 보장한다"

여러 단계로 이루어진 작업은 간단한 계획을 먼저 제시한다:
```
1. [단계] → 검증: [확인 방법]
2. [단계] → 검증: [확인 방법]
3. [단계] → 검증: [확인 방법]
```

명확한 성공 기준이 있으면 독립적으로 반복 작업할 수 있다. "동작하게 만들어라"처럼 모호한 기준은 계속되는 확인 질문을 필요로 한다.

---

**이 지침이 잘 작동하고 있다는 신호:** 불필요한 변경이 diff에 줄어들고, 과도한 설계로 인한 재작성이 줄어들며, 구현 후가 아니라 구현 전에 명확화 질문이 나온다.
