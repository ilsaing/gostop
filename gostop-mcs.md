# gostop-mcs.html 설계 논의 노트

`gostop-mcs.html`의 결정화(determinization)·후보 생성·보상 함수 개선 방향을 논의하고
실제로 구현·검증한 내용을 정리한다. 5절에 통계 검증 결과, 6절에 결론과 향후 방향이 있다.

## 1. 문제: 결정화가 "손패 vs 덱" 카드 분포의 편향을 반영하지 않음

### 현재 구현 (`determinizeWorld`, 2700줄)

안 보이는 카드 전체를 한 번에 섞은 뒤, 각 상대의 "안 보이는 손패 장수"만큼 순서대로 잘라
배분하고 나머지를 덱으로 채운다. 즉 특정 카드가 어느 슬롯(상대 A 손패 / 상대 B 손패 / 덱)에
들어갈 확률은 **그 슬롯에 남은 장수에 비례**한다 — 손패 4장 대 덱 15장이면 15:4 비율로
배분된다는 뜻. 흔들기로 공개된 패는 셔플 대상에서 빼고 고정한다.

이 부분(슬롯 크기 비례)은 이미 맞게 구현되어 있다 — "손패냐 덱이냐를 50:50으로 본다"는
식의 왜곡은 아니다.

### 실제로 남아있는 왜곡

이 게임 규칙상 손패는 처음 7장 배분 이후 오직 줄어들기만 한다(보너스 카드로 덱에서 새로
들어오는 드문 예외 제외). 즉 지금 상대 손에 남아있는 미공개 카드는 "그 상대가 매 턴 무엇을
낼지 스스로 골라온 결과"다. 그리고 그 선택 로직(휴리스틱이든 MCS든) 자체가 "쌀 걸 먼저
버리고 비싼 걸 아낀다"는 경향을 갖고 있으므로(이번 세션에서 고친 `aiChooseHandCard`의
폴백 discard 로직 버그가 정확히 이 원리를 다루고 있었음 — 카드 원가치를 반영 안 하면 비싼
카드도 헐값에 버려짐), 라운드가 진행될수록 상대 손에 남은 카드들은 **생존 편향
(survivorship bias)**을 거쳐 카드 가치 분포상 평균보다 위로 치우친 표본이 된다.

반면 덱에 남은 카드는 누구의 선택도 거치지 않은, 원래 그대로의 균등 분포다.

`determinizeWorld`는 안 보이는 카드 풀을 손패 슬롯/덱 슬롯에 나눌 때 이 차이를 전혀
반영하지 않는다 — 슬롯 "개수"에만 비례하고, 손패 슬롯이 평균적으로 더 비싼 카드를 담아야
한다는 건 고려하지 않는다. 그 결과:

- 시뮬레이션 속 "가상 상대 손패"가 실제보다 약해 보일 수 있다(값진 카드가 우연히 덱으로
  새어나갈 수 있으므로).
- 반대로 "안 보이는 덱 카드"는 실제보다 세 보이는 쪽으로 왜곡될 수 있다.
- 이 편향은 라운드가 진행될수록(=선택이 누적될수록) 더 커진다.

## 2. 고려했던 해결 방향 3가지

1. **가치 가중 샘플링**: 손패 슬롯을 채울 때 균등추출 대신 카드 가치(`cardWeight`) 가중
   비복원추출로 뽑아서, 비싼 카드가 손패 슬롯에 더 자주 배정되게 한다.
2. **행동 이력 기반 사후확률 추정**: 상대가 그동안 "버릴 수 있었는데 안 버린"/"먹을 수
   있었는데 안 먹은" 실제 이력을 보고 훨씬 정교한 사후확률을 매 결정화마다 계산.
3. **1번 정도의 근사만 넣고 정확도 대비 구현 복잡도를 지켜보기**.

### 결론: 2번은 기각, 3번(=1번의 가벼운 근사) 방향으로

진짜 잘하는 사람이라면 2번까지 고려하겠지만, 2번은 결정화를 만들 때마다(K=100번 반복되는
매 판단 지점마다) 각 상대의 행동 이력을 다시 훑어야 해서 지연시간 제약과 정면으로 부딪힌다.

1번(가치 가중 샘플링)은 계산 비용이 거의 공짜에 가깝다 — 현재처럼 통째로 셔플 후 자르는
대신, 가중치 있는 비복원추출(예: Efraimidis–Spirakis 방식: 각 카드에
`random()^(1/weight)` 키를 매겨 상위 N개를 뽑음)로 바꿔도 시간복잡도는 셔플과 동일하게
O(n log n)이라 지연시간에 거의 영향이 없다.

**따라서 2번은 포기하고, 1번(=3번 근사) 방향으로 진행한다.**

## 3. 남은 설계 포인트: 편향 강도(지수 α)를 어떻게 잡을지

가중치를 `cardWeight(card)^α` 형태로 준다고 할 때(α=0이면 균등추출과 동일, α가 클수록
비싼 카드가 손패 슬롯에 쏠리는 정도가 강해짐):

- α는 **라운드 진행도**(그 상대가 원래 손패 7장 중 이미 몇 장을 냈는지)에 연동돼야 자연스럽다.
  아직 하나도 안 낸 시점(초반)엔 편향이 거의 없어야 하고(선택이 누적 안 됐으므로), 7장 중
  5~6장을 이미 낸 후반부일수록 편향이 강해야 한다.
  - 예시 스케일링 함수: `α = α_max * (7 - 남은장수) / 7`
- **α와 남은 장수의 관계를 정확히 얼마나 세게 줄지(비례 / 제곱근 / 제곱 등)는 직관만으로
  정할 수 없고, 통계 검증이 필요하다** — 이번 세션에서 낸 결론.

## 4. 실측 사례: deep lookahead 판단이 K=100에서도 노이즈에 크게 흔들림

실전 로그에서 "손패 5장, 11월 피로 광을 바로 먹을 수 있는데도 3월로 흔들며 그냥 버림"이라는
의아한 선택이 나와 원인을 추적했다.

### 재현 결과

그 순간의 실제 상태(손패 3월광·3월피·3월피·11월피·11월피, 바닥에 11월광 포함)를 그대로
재구성해서 `buildTurnActionCandidates`를 호출해보니, 후보 생성 자체는 정상이었다 — "11월
피로 광 먹기"와 "3월 피 버리기" 둘 다 정확히 후보에 올라왔다. 문제는 그 다음이었다: 완전히
동일한 상태에서 `mcsChoose`의 평가(K=100)를 8번 독립적으로 반복하니 승자가 계속 바뀌었다.

| 시도 | 11월(광 먹기) | 3월(버림) | 승자 |
|---|---|---|---|
| 1 | 5.70 | 7.15 | 3월 |
| 2 | 10.55 | 10.27 | 11월 |
| 3 | 7.32 | 6.00 | 11월 |
| 4 | 7.57 | 8.59 | 3월 |
| 5 | 11.57 | 8.61 | 11월 |
| 6 | 9.71 | 9.33 | 11월 |
| 7 | 12.41 | 12.42 | 3월 |
| 8 | 11.24 | 9.06 | 11월 |

8번 중 3번 승자가 뒤바뀐다 — 두 후보의 실제 가치가 가깝고, K=100 표본으로는 어느 쪽이
진짜 나은지 안정적으로 못 가릴 만큼 분산이 크다는 뜻이다. 즉 이 판단은 "잘못된 판단"이
아니라 노이즈가 결과를 좌우하는 경계 상황이었다.

### 원인: lookahead가 노이즈를 두 겹으로 쌓는다

이 상황은 손패 5장이라 `mcsLookaheadForHand`가 deep lookahead(=3)를 건다. lookahead가
걸리면 바깥쪽 K=100의 세계선 하나하나가 그 안에서 다시 `mcsChoose`를 중첩 호출해
`MCS_LOOKAHEAD_K`(=6)로 재결정화한다 — 바깥 세계선 자체의 무작위성 위에 중첩 판단의
무작위성이 한 겹 더 쌓이는 구조.

- **바깥쪽 K를 늘리면** 이 두 겹의 노이즈를 한 번에 평균 내서 줄인다(표본 100개 자체가
  늘어나므로).
- **중첩 K를 늘리면** 안쪽 노이즈 한 겹만 줄고, 바깥쪽 표본 수(100개)는 그대로라 전체
  안정성 개선 효과가 더 작다.

기존에 검증된 "K=50/100/200, 유의미한 차이 없음"(`gostop-mcs.html` 3166줄 주석)은
lookahead가 안 걸리는 손패 6장 이상 판단까지 전부 섞은 평균이었을 가능성이 높다 — 그런
판단은 애초에 분산이 작아 K를 늘려봐야 표가 안 났을 것이다. **lookahead가 걸리는 손패
5장 이하 구간만 따로 떼면 결론이 달라질 수 있다.**

### 제안 방향

`MCS_ROLLOUTS`(모든 판단 공통)를 무조건 올리기보다, 이미 고/스톱 전용으로 `MCS_ROLLOUTS_GOSTOP`을
따로 둔 선례를 그대로 따라 **lookahead가 걸리는 turnAction 전용으로 더 큰 K**(가칭
`MCS_ROLLOUTS_LOOKAHEAD`)를 별도로 두는 쪽이 더 정확하다. 이러면 지연시간도 정말 필요한
구간(손패 5장 이하)에만 늘어난다.

## 5. 구현 및 통계 검증 결과 (일요일 세션)

위 3·4절에서 논의한 방향들을 실제로 구현하고, 순수 휴리스틱(`statSeatMcs=-1`) 대비
MCS 좌석의 라운드당 평균 코인 증감을 공통난수 자기대국(같은 딜/같은 플레이 시드를
A/B 양쪽에 그대로 재사용)으로 비교했다. 총 10가지를 서로 다른 방향으로 시도했다.

### 구현한 것들

- **가치 가중 결정화** (`determinizeWorld`/`weightedTake`/`determinizeCardWeight`,
  `DETERMINIZE_VALUE_WEIGHTED`): 2·3절 논의 그대로 — 손패 슬롯을 `cardWeight^α`
  가중 비복원추출(Efraimidis–Spirakis)로 채움. α는 `DETERMINIZE_ALPHA_MAX * (7-남은장수)/7`.
- **방어적 가중치** (`DETERMINIZE_DEFENSIVE_WEIGHT`): 카드 원가치에 더해 "이 카드를
  상대가 흘리면 얼마나 위험한가"(`opponentRiskIfLeft`)도 반영, 그 월이 이미 대부분
  설명된(굳은자) 경우엔 함께 약화(`settledScale`).
- **무투표 당선**(`aiChooseHandCard`의 `forcedOut` 파라미터 + `buildTurnActionCandidates`):
  candidate-2가 (a) 폭탄 대기 트리플·확정 뻑 미루기·흔들기 대기 같은 "대안 없을 때만
  뚫리는 보호"를 excludeMonths 때문에 억지로 뚫고 나온 가짜 후보이거나, (b) candidate-1이
  이미 즉시 득점하거나 상대 즉시 득점을 막는 명백히 우선인 패(decisive)면, candidate-2를
  아예 만들지 않고 1개짜리 후보로 끝낸다(`mcsChoose`가 후보 1개면 시뮬레이션 없이 그대로 채택).
- **lookahead 없이 넓게 + K를 키움**: `MCS_TURNACTION_DEEP_LOOKAHEAD=0`(중첩 안 함) +
  `MCS_TURNACTION_HAND_THRESHOLD`를 5(기존 범위) 또는 7(손패 전체)로, `MCS_ROLLOUTS`를
  500~2000까지. lookahead 중첩을 없애니 같은 지연 예산으로 K를 몇 배는 더 키울 수 있었다
  (예: 손패 7장 최악의 경우 K=500이 lookahead=3·K=100보다도 6배 빠름).
- **horizon 절단**(`MCS_HORIZON_TURNS`, `interimValue`): 라운드 끝까지 안 굴리고 지정한
  턴 수만큼만 진행한 뒤, 코인 정산 대신 "내 점수 - 상대 중 최고 점수"를 대체 보상으로 씀 —
  결정 이후 먼 훗날의 무작위 결과가 신호를 희석시키는 걸 줄이려는 시도.
- **보상 셰이핑**(`MCS_REWARD_SHAPING_WEIGHT`): 후보 자체의 `immediateScore`(이번 캡처로
  즉시 득점하는지)·`blockRisk`(안 가져가면 상대가 위험한 정도) — 이미 휴리스틱이 정확히
  아는 값 — 를 시뮬레이션 평균에 직접 더함.

### 검증 결과 (전부 N=300, 공통난수 자기대국, 코인 증감 기준)

| # | 시도 | z | 유의미? |
|---|---|---|---|
| 0 | 기존 베이스라인(K=100, lookahead=3, 아무 수정 없음) | -0.93 | 아니오 |
| 1 | 결정화: 가치 가중 α=1.5 | -0.40 | 아니오 |
| 2 | 결정화: 가치 가중 α=4 | -0.37 | 아니오 |
| 3 | 결정화: 가치+방어 가중 α=1.5 | **+0.21** | 아니오(유일한 플러스) |
| 4 | 무투표 당선: 폭탄/뻑미루기/흔들기대기만 | -1.80 | 아니오(근접) |
| 5 | 무투표 당선: +즉시득점/방어긴급까지 확장 | -0.96 | 아니오 |
| 6 | 결합(방어가중 결정화 + 무투표당선) | -1.43 | 아니오 |
| 7 | 결합 + K=2000·lookahead=0·손패5장부터 | **-2.18** | **네(나쁜 쪽)** |
| 8 | horizon=6턴 절단, K=1000, lookahead=0 | -1.93 | 아니오(근접) |
| 9 | 보상 셰이핑(immediateScore×3+blockRisk), K=1000 | **-2.62** | **네(가장 나쁨)** |

### 결론

**10가지 중 단 하나도 유의미하게 개선되지 않았다. 유의미했던 두 번(#7, #9)은 둘 다
악화였다.** 특히 #7(K=2000까지 올려 노이즈를 사실상 없앤 뒤에도 유의미하게 나쁨)과
#9(이미 정답을 아는 신호를 보상에 직접 주입했는데도 가장 나쁨)는 "노이즈 때문에 가끔
잘못 고른다"는 가설과 "시뮬레이션이 알아내는 데 실패한다"는 가설을 둘 다 뒤집는
증거다 — 노이즈를 없애도, 답을 알려줘도 개선이 없었다.

가능한 해석: 이 `aiChooseHandCard` 휴리스틱은 이미 이번 프로젝트 전체에서 여러 차례
(3~7차) 통계 튜닝을 거친 상태라, MCS가 재비교할 만한 "실수"가 애초에 거의 안 남아있을
가능성이 크다. 즉 문제는 MCS 구현의 세부 사항(결정화 정확도, 노이즈, 보상 함수)이
아니라, **애초에 candidate-1(휴리스틱 1순위)을 candidate-2와 재비교하는 구조 자체가
이 정도로 잘 튜닝된 휴리스틱 앞에서는 득보다 실이 크다**는 쪽에 무게가 실린다.

## 6. 앞으로의 방향에 대한 생각

- 이 세션에서 시도한 방향(결정화 정교화, 후보 생성 개선, 보상 함수 개선)은 전부
  "MCS가 candidate-1 vs candidate-2를 재비교하는 절차 자체는 유지한 채, 그 재비교를
  더 정확하게 만들기"였다. 그런데 그 재비교 자체가 순손실이라면, 더 정교하게 만들어도
  한계가 있을 수 있다.
- 다음에 다시 판다면, "재비교를 더 정확하게" 대신 **"언제 재비교할지 자체를 더 보수적으로
  줄이기"**(무투표 당선의 범위를 더 넓히기 — 예: candidate-1과 candidate-2의 baseWeight
  차이가 일정 기준 이상이면 아예 비교 안 함) 쪽이 이론적으로 더 맞을 수 있다. 다만 #7이
  이미 "노이즈를 없애도 나쁘다"를 보여줬으므로, 이 방향도 큰 개선을 기대하긴 어렵다.
- 가장 솔직한 결론은: **이 프로젝트의 MCS는 지금까지 시도한 범위 내에서 순수 휴리스틱을
  이긴 적이 없다.** 더 시간을 쓰기 전에, "MCS를 계속 개선할 가치가 있는가" 자체를
  다시 논의하는 게 나을 수 있다 — 예를 들어 MCS를 포기하고 휴리스틱 자체의 남은 개선
  여지(예: 이번 세션에서 찾은 12월열끗/버릴 카드 가중치 버그 같은 것)에 집중하는 대안도
  있다.
